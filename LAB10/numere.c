#include <stdio.h>
#include <stdlib.h>



int main(void) {
    FILE* file = fopen("data.in", "r");

    int n;
    fscanf(file, "%d", &n);

    int* arr = 
    for(int i = 0; i<n; i++) fscanf(file, "%d", &arr[i]);

    return 0;
}